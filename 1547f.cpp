#include <bits/stdc++.h>
using namespace std;

int gcd(int a,int b)
{
    while (b!=0)
    {
        int r;
        r=a%b;
        a=b;b=r;
    }
    return a;
}


int main()
{
    int t; cin >> t;
    while (t!=0)
    {
        int n; cin >> n;
        int nhan[n]; 
        for (int i =0; i < n; ++i) cin >> nhan[i];
        int count(0);
        if (gcd(nhan[st.size()-1],))
        for (int i = 0; i < n; i++) if ((gcd(nhan[i],nhan[i+1]==1))  && (gcd(nhani)))
        
        --t;
    }
    
    return 0;
}
